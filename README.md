# Airbnb_Listings
Implement a page that displays and loads all the first 50 listings from the JSON file using AJAX (JavaScript fetch and await). The page should show the listing name, description, amenities, host (name and photo), price, thumbnail and some creative addition that separates your code from others.


#Deployed at: https://js-dom-sukanyasudhirshete.netlify.app/
